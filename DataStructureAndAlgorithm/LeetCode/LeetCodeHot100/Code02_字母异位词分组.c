/*给你一个字符串数组，请你将 字母异位词 组合在一起。可以按任意顺序返回结果列表。

字母异位词 是由重新排列源单词的所有字母得到的一个新单词。

CodeNum:49
*/


#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

// char*** groupAnagrams(char** strs, int strsSize, int* returnSize, int** returnColumnSizes) {
    

// }


int main(){

    char str1[][6] = {"eat", "tea", "tan", "ate", "nat", "bat"};
    char str[] = "fkgjh";

    printf("%s\n", str);
    printf("%c\n", str1[0][1]);
    int a = INT_MIN;

    return 0;
}